rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Lectura pública de picks; escritura solo mediante Cloud Functions
    match /picks/{pickId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
